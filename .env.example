# Ralsha Backend API base URL (server-side only — never expose to browser)
# Example value below will NOT work from a deployed Vercel environment unless
# the Ralsha backend is actually running at that URL.
# The real deployed backend base URL must be confirmed before preview/production deployment.
RALSHA_BACKEND_API_URL=https://example.com
